// Component-only plugin entry point.
//
// BYHEAVEN's Waline comments component is shipped via the `./components` export
// and declared in package.json's `quartz.components` manifest, so Quartz loads
// and registers it automatically. This file has no factory and no side effects.
export {}
